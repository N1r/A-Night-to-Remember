# Cookie Manager WebUI 使用说明

## 功能特色

- ✅ **现代化UI设计** - 基于 NiceGUI 的响应式 web 界面
- 🍪 **多平台支持** - 抖音、B站、快手、小红书、视频号
- 🔐 **Cookie 状态检测** - 实时验证 Cookie 是否有效
- 📱 **扫码登录** - 一键启动浏览器获取登录二维码
- 🗑️ **Cookie 管理** - 删除不需要的 Cookie 文件

## 安装依赖

```bash
cd /home/nir/A-Night-to-Remember/B_project/Code/Kimi_V

# 创建虚拟环境
uv venv .venv_webui

# 安装依赖
.venv_webui/bin/python -m pip install nicegui rich ruamel.yaml
```

## 启动 WebUI

### 方法 1: 使用启动脚本
```bash
chmod +x start_webui.sh
./start_webui.sh
```

### 方法 2: 直接运行
```bash
.venv_webui/bin/python simple_cookie_manager.py
```

访问 http://localhost:8080

## 使用流程

### 1. 扫码登录
1. 点击平台卡片上的「扫码登录」按钮
2. 浏览器启动后显示登录二维码
3. 使用对应平台 App 扫描二维码
4. 登录成功后 Cookie 自动保存

### 2. 验证 Cookie
点击平台卡片上的「验证」按钮，系统会检查 Cookie 是否仍然有效

### 3. 删除 Cookie
点击平台卡片上的「删除」按钮，清除选平台的 Cookie 文件

### 4. 刷新状态
点击顶部的「刷新状态」按钮，更新所有平台的 Cookie 状态

## 项目结构

```
Kimi_V/
├── simple_cookie_manager.py    # WebUI 主程序
├── start_webui.sh              # 启动脚本
├── requirements_webui.txt      # WebUI 依赖
├── .venv_webui/               # 虚拟环境
│   └── lib/python*/site-packages/
├── output/                     # 二维码截图保存目录
└── storage/cookies/           # Cookie 文件存储目录
```

## 技术栈

- **NiceGUI** - Python Web UI 框架
- **Playwright** - 浏览器自动化
- **Rich** - 美化终端输出

## 注意事项

1. 首次扫码登录会创建浏览器配置文件
2. Cookie 文件保存在 `storage/cookies/` 目录
3. 建议定期验证 Cookie 有效期
4. 如需重新登录，先删除旧的 Cookie 文件

## 开发

```bash
# 代码语法检查
.venv_webui/bin/python -m py_compile simple_cookie_manager.py

# 运行测试
.venv_webui/bin/python simple_cookie_manager.py
```

## 更新日志

### v1.0.0 (2026-02-26)
- ✅ 初始版本发布
- ✅ 支持多平台 Cookie 管理
- ✅ 实时状态检测
- ✅ 扫码登录功能
